package com.fetoxdevelopments.optix.api.struct;

import org.bridj.Pointer;
import org.bridj.TypedPointer;

public class RTtexturesampler
  extends TypedPointer
{
  public RTtexturesampler(long address)
  {
    super(address);
  }

  public RTtexturesampler(Pointer address)
  {
    super(address);
  }
}
