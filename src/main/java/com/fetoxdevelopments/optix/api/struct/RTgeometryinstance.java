package com.fetoxdevelopments.optix.api.struct;

import org.bridj.Pointer;
import org.bridj.TypedPointer;

public class RTgeometryinstance
  extends TypedPointer
{
  public RTgeometryinstance(long address)
  {
    super(address);
  }

  public RTgeometryinstance(Pointer address)
  {
    super(address);
  }
}
